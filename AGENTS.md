# CSV → Markdown 表格

CSV 粘进去，Markdown 表格出来。纯前端单页，没有后端，数据不离开浏览器。

## 命令

```
node scripts/verify.mjs        # 快闸门：零依赖、离线，几十秒。改完任何东西都必须跑
node scripts/verify-web.mjs    # 浏览器闸门：需要 npm install + npx playwright install chromium
```

快闸门共 43 条检查（behavior 20 / structure 19 / mutant 4），浏览器闸门共 12 条检查。
这两个数字各有一条等号断言钉着：`docs-count-crosscheck` 钉快闸门，`web-count-crosscheck`
钉浏览器闸门（它数的是 `verify-web.mjs` 源码里真实的 `check()` 注册点，先剥注释与字符串）。
后一条是补上来的:文档一度声称两个数字都有断言，而实际只钉了前一个。

## 结构

```
index.html              单页外壳
src/engine.mjs          纯核心：解析 + 渲染。不碰 I/O
src/app.mjs             DOM 外壳。只搬数据
scripts/verify.mjs      快闸门
scripts/verify-web.mjs  浏览器闸门（Playwright）
scripts/compose-report.mjs   把 report.json 合成 PR / commit 评论
scripts/write-heartbeat.mjs  定时运行的正向痕迹
manifest.json           登记表：文件集合、闸门名单、marker、cron、耦合参数、义务
heartbeat.json          由 CI 写，不要手改
```

## 铁律

1. **改完必须跑 `node scripts/verify.mjs`。** 没跑过的改动不许推。
2. **`src/engine.mjs` 是纯的**：不读文件、不碰 DOM、不发网络、不用系统时间、
   不用未播种的随机。有扫描器在守，它先剥注释与字符串再找，所以注释里出现
   `document` 这个词不会误报。回报是：同样输入必然同样输出、毫秒内能压几万行、
   同一份逻辑换任意外壳复用，而且「撤销 / 历史」这类功能几乎白送。
3. **`snapshot()` 的字段可以增加，不许删改。** 闸门认这些名字，一次重构把它们
   改掉就等于把闸门弄哑了。
4. **页面元素认 id，不认显示名。** 改文案必须是响亮的，不能是承重的。
5. **新增可验证行为，顺手补一条断言。** 并且问自己两句：这个功能完全没实现的话
   这条会不会失败（会失败才不是空断言）；我在乎的属性里有哪一个完全没有断言在看
   （全绿从来不等于覆盖全）。
6. **新增流水线必须接回写。** 有一条从目录枚举的白名单断言在守，新加的默认不通过。
7. **阈值不许拍脑袋。** 先跑一次拿实测值，再往下留三倍余量；没有实测值就别写这条
   断言。报告里把「量出来的」和「猜的」分开写。
8. **散文里的数字要么由机器生成，要么配一条等号断言。** 手写 + 靠人记得改 = 一定漂。
9. **CI 环境的问题在 CI 配置里解决，别改产品代码去迁就尺子。**
10. **红了先怀疑尺子，不是产品。** 已经兑现过两次：`tee-needs-pipefail` 第一版没
    剥 YAML 注释，被一句**解释这条规矩本身**的注释判红了；`screenshot-differs`
    第一版拿一个零尺寸元素去截图，在 CI 上超时。两次的根因都在闸门自己身上。

## 耦合参数：改一个必须重算另一个

| 一头 | 另一头 | 守它的断言 |
| --- | --- | --- |
| `ALIGN_RULE` 的字符串长度 | `toMarkdown` 的列宽下限 | `alignWidthCoupling` |
| `cron` 的频率 | `max_heartbeat_age_days` | `cron-coupling` |
| `report` 传进去的 marker | `attest` 回头查的 marker | `marker-coupling` |
| `src/` 下的实际文件 | `manifest.json` 的 `src` | `src-manifest-equal` |
| `report-*` 产物名 | `manifest.json` 的 `gates` | `gates-manifest-equal` |
| 快闸门注册的条数 | AGENTS.md / README.md 里那句话 | `docs-count-crosscheck` |
| `verify-web.mjs` 里的 `check()` 注册点 | AGENTS.md / README.md 里那句话 | `web-count-crosscheck` |

`marker` 注定要写两遍：可复用 workflow 的 `with:` 读不到 `env` 上下文。
所以它是一组有断言看守的耦合参数，不是可以「记得同时改两处」的东西。

## 闸门为什么长这样

**报告必须自带证据。** CI 的原始日志我读不到，所以每条失败都要把期望值、实际值、
以及 stdout 尾巴塞进那条评论。判断标准：只看那条评论，能不能定位到根因？

**两个闸门不许读同一个 oracle。** `verify-web.mjs` 的期望值是**手写的字面量**，
不是 engine 算出来的。否则引擎一旦错，两边会一起错而且错得完全一致，报告看起来
像「两个独立闸门都同意了」，实际只有一个真值来源。

**变异体证明断言不是装饰。** 四条：去掉竖线转义、把解析降级成朴素 split、把量宽
挪进渲染循环、让分隔符探测器不认引号。每条都先断言「替换真的发生了」，然后把
**同一个检查器**套在变异体上，要求它必须红。

**`cellVisits` 是结构性的复杂度守卫，不是计时基准。** 它恒等于 行 × 列 × 2；谁把
量宽挪进渲染循环，这个等号当场对不上。三条数据和三万条在计时上看不出区别。
`charsScanned` 只是个诚实的计量口 —— 单遍扫描下它恒等于输入长度，拿它当断言是空的。

**心跳是正向痕迹。** 平台会因为仓库长期不活跃**静默停用定时工作流**，而那时
workflow 文件一个字都不会变 —— 「读配置确认 cron 还在」会在它本该抓住的那次失效上
保持绿色。「没有坏消息」和「已经死了」长得一样，带时间戳的痕迹不会。
防自触发靠结构（只在 schedule / 显式手动时写），不靠提交信息里的字符串。

**零尺寸元素截不了图。** 空状态下 `#preview` 里一个节点都没有，元素不可见，
无头浏览器会等它稳定直到超时。**零尺寸才是「没画表格」的可观测形式，不是一张空图。**
所以截图只在两个都有内容的状态（2 行 vs 4 行）之间比，而且两边只差在行数上 ——
拿整个面板去截的话，textarea 也在变，「表格压根没渲染」那个毋经会活下来。
空状态改成断言盒子为零，那是它的负向孪生。

**`attest` 是独立的送达核对。** `report` job 内部会读回评论，但那是同一个写入者在读
自己；如果 `report` 压根没被唤起，那个读回也不存在，而不存在的报告不会喊。
`attest` 两条通道都查、钉在本次短 SHA 与 run id 上、轮询而不睡一觉、并且在一个
必然不存在的 marker 上自证返回 0 条。它不占用 `report-*` 与 `stdout-*` 命名空间。

## 测不出来的

这一节要经常收窄，收窄比往里加更值得记一笔。一条比事实更宽的「测不出来」，
会让人放弃一条其实走得通的路。

- **上游 `report.yml` 钉的是 `@main`，本仓内部看不见它漂移。** 这是有意的：pin SHA
  会让六个消费者停在不同版本，两边都绿而行为已经不同。守它的东西住在
  `supercubegame/clickup-brain-backup`（归档拷贝 + 读回期限），不在这里。
- **中文字形有没有真的画出来，验不到。** `cjk-font-available` 证明的是**字体可加载**，
  不是栅格化正确 —— 方块字也有像素，探针像素数、背景色令牌、截图哈希全都对得上。
  这一半已经从「整件事都拿不到」收窄成「字体这一半拿到了，字形那一半还没有」。
  真要补，需要 OCR，而快闸门是零依赖的。
- **浏览器闸门在沙箱里跑不了，CI 是它唯一的尺子。** 没有浏览器二进制也拉不下来，
  所以对它的任何修改都是「本地只验了语法」，真正的判定在 CI 上。不要把它当成
  跑过的。
- **CI 的用量 / 计费页面读不到。** 仓库是公开的，托管 runner 目前免费无限，所以
  这件事此刻不咬人。但「每月烧多少」只能是估算，不许写成实测。
- **链条最后一环没人看着。** `attest` 检查 `report`，而 `attest` 自己只由 CI 的红绿
  和你来看。真正在链条外面的只有你和 CI 的红绿,这句不假装它闭合了。

## 挂着的义务

登记在 `manifest.json` 的 `obligations` 里，有判据、有期限，`obligations-registered`
在守。期限到了只有两个正确反应，都不是把日期往后挪：真的做完了（删掉记录），
或者确认做不到（挪进上面那一节）。

「从此以后都要保持」的那种，履行的正确动作是**先把完成判据转成一条常驻断言，
然后才删记录**。直接删等于把看守和待办一起扔了。
